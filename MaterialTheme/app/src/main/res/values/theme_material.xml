<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base"></style>
    <style name="MyMaterialTheme2" parent="MyMaterialTheme.Second"></style>
    <!-- 各API通用的基底theme -->
    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <!-- 不需要 ActionBar 了,因為改用Toolbar -->
        <item name="windowActionBar">false</item>

        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primaryDark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:textColor">@color/color_text</item>
        <item name="android:textColorPrimary">@color/color_primary_text</item>
        <item name="android:windowBackground">@color/color_window_background</item>
    </style>

    <style name="MyMaterialPopupTheme">
        <item name="android:colorBackground">?attr/colorAccent</item>
        <item name="android:textColor">@color/color_primary_text</item>
    </style>

    <style name="MyMaterialTheme.Second" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/color_primary2</item>
        <item name="colorPrimaryDark">@color/color_primaryDark2</item>
        <item name="colorAccent">@color/color_accent2</item>
        <item name="android:textColor">@color/color_text2</item>
        <item name="android:textColorPrimary">@color/color_primary_text2</item>
        <item name="android:windowBackground">@color/md_blue_grey_700</item>
    </style>

</resources>